---
import { getCollection } from "astro:content";
import { Picture } from "@astrojs/image/components";
import Layout from "@layouts/Layout.astro";
import Container from "@components/container.astro";
import Sectionhead from "@components/sectionhead.astro";

// Filter team entries with 'draft: false' & date before current date
const publishedTeamMembers = await getCollection("team", ({ data }) => {
  return !data.draft && data.publishDate < new Date();
});
---

<Layout title="About">
  <Container>
    <Sectionhead>
      <Fragment slot="title">Nosotros</Fragment>
      <Fragment slot="desc">Soluciones Integrales de Diseño y Desarrollo Web</Fragment>
    </Sectionhead>

    <div class="flex flex-col gap-3 mx-auto max-w-4xl mt-16">
      <h2 class="font-bold text-3xl text-gray-800">
     Conoce al equipo detras de Web Nexus</h2>
      <p class="text-lg leading-relaxed text-slate-500">
        Nuestro equipo Multidisciplinario se compone por Ingenieros Informáticos, Mercadólogos, Diseñadores Gráficos, Administrativos, Consultores, Fotógrafos, Publicistas, Especialistas SEO, y Redactores.</p>
    </div>
    <div class="grid md:grid-cols-3 gap-10 mx-auto max-w-4xl mt-12">
      {
        publishedTeamMembers.map((teamMemberEntry) => (
          <div class="group">
            <div class="w-full aspect-square">
              <Picture
                src={teamMemberEntry.data.avatar.src}
                alt={teamMemberEntry.data.avatar.alt}
                sizes="(max-width: 800px) 100vw, 400px"
                widths={[200, 400]}
                aspectRatio="1:1"
                background="#ffffff"
                fit="cover"
                position="center"
                class="w-full rounded transition group-hover:-translate-y-1 group-hover:shadow-xl"
              />
            </div>

            <div class="mt-4 text-center">
              <h2 class="text-lg text-gray-800">
                {teamMemberEntry.data.name}
              </h2>
              <h3 class="text-sm text-slate-500">
                {teamMemberEntry.data.title}
              </h3>
            </div>
          </div>
        ))
      }
    </div>
    <div class="custom-container flex flex-col gap-3 mx-auto max-w-4xl mt-16">
      <!-- Nueva sección para Misión, Visión, Filosofía y Valores -->
      <h2 class="font-bold text-3xl text-gray-800">Misión</h2>
      <p class="text-lg leading-relaxed text-slate-500">
        Ofrecer soluciones digitales de alta calidad que impulsen el crecimiento de nuestros clientes, manteniendo la innovación y excelencia en cada proyecto.
      </p>

      <h2 class="font-bold text-3xl text-gray-800">Visión</h2>
      <p class="text-lg leading-relaxed text-slate-500">
        Ser líderes en el mercado de desarrollo web y tecnología digital, reconocidos por nuestra creatividad, eficiencia y compromiso con el cliente.
      </p>

      <h2 class="font-bold text-3xl text-gray-800">Filosofía</h2>
      <p class="text-lg leading-relaxed text-slate-500">
        Creemos en la mejora continua y en el potencial de nuestro equipo para alcanzar estándares cada vez más altos de calidad y servicio al cliente.
      </p>

      <h2 class="font-bold text-3xl text-gray-800">Valores</h2>
      <ul class="text-lg leading-relaxed text-slate-500 list-disc ml-6">
        <li>Innovación</li>
        <li>Colaboración</li>
        <li>Integridad</li>
        <li>Excelencia</li>
        <li>Compromiso</li>
        <li>Agilidad</li>
        <li>Respeto</li>
        <li>Pasión</li>
      </ul>
    </div>
  </Container>
</Layout>


<style>
  .custom-container {
    background-color: #f3f4f6;
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }
  .custom-container h2 {
    margin-top: 20px;
    margin-bottom: 15px;
  }
  .custom-container p,
  .custom-container ul {
    margin-bottom: 15px;
  }
</style>